# Plugin Policy

Owns the read-only Dispatch Plugin Standard v1 conformance auditor used for domain plugins under `plugins/`.

Dispatch Core itself is not evaluated as a plugin and does not carry a `dispatch-plugin.yaml` manifest.
